var Store= function () {
	// init
}

Store.prototype.get = function(key) {
	// get value
};

Store.prototype.set = function(key, value, opts) {
	// set key/value
};

Store.prototype.destroy = function(key) {
	// destroy a key/value
};

module.exports= Store;